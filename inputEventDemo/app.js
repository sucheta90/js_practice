/*
const username = document.querySelector('#username');
const h1 = document.querySelector('h1');
username.addEventListener('input', function(e){
    if (!this.value){
        h1.innerText = 'Enter Your Username'
    }
    else{
    h1.innerText = `Welcome! ${username.value}`;}
 
})
*/
const ascii_val_A = 'A'.charCodeAt(0)
console.log(`ascii value of A ${ascii_val_A}`)

const ascii_val_Z = 'Z'.charCodeAt(0)
console.log(`ascii value of Z ${ascii_val_Z}`)

const ascii_val_a = 'a'.charCodeAt(0)
console.log(`ascii value of a ${ascii_val_a}`)

const ascii_val_z = 'z'.charCodeAt(0)
console.log(`ascii value of z ${ascii_val_z}`)


console.log(`ascii code 70 represents ${String.fromCharCode(70)}`)

/*
input = ['A', 'x', 'h', 'U', 'B']
output = ['a', 'X', 'H', 'u', 'b']
 */